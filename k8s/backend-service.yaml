apiVersion: v1
kind: Service

metadata:
  name: backend-service
  namespace: employee-management

spec:
  selector:
    app: employee-backend

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080

  type: ClusterIP
